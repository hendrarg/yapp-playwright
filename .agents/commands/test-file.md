---
description: Run a specific test file (usage: /test-file tests/buyer/explore.spec.ts)
---

Run a single Playwright test file.

Command: `npx playwright test $1 --project=chromium`

Output:
!`npx playwright test $1 --project=chromium`

Summarize results. If it fails, analyze the error and suggest fixes.
