---
description: Open Playwright UI mode for interactive test debugging
---

Run Playwright in UI mode for interactive debugging.

```
npx playwright test --ui
```

Note: This opens a browser-based UI. The session will remain active until manually closed.
